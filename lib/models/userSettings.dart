import 'package:flutter/material.dart';

class UserSettings{
  Color color;
  IconData icon;
  String name;

  UserSettings({
    required this.color,
    required this.icon,
    required this.name,
  });
}